add_subdirectory(cxxurl)
add_subdirectory(win32-compat)

if(NOT TARGET dep_blake3)
  add_subdirectory(blake3)
endif()
if(HTTP_STORAGE_BACKEND OR OCI_STORAGE_BACKEND OR GHA_STORAGE_BACKEND)
  if(NOT TARGET dep_cpphttplib)
    add_subdirectory(cpp-httplib)
  endif()
endif()
if(NOT TARGET dep_fmt)
  add_subdirectory(fmt)
endif()
if(NOT TARGET dep_tlexpected)
  add_subdirectory(tl-expected)
endif()
if(NOT TARGET dep_xxhash)
  add_subdirectory(xxhash)
endif()
if(ENABLE_TESTING AND NOT TARGET dep_doctest)
  add_subdirectory(doctest)
endif()
if(REDIS_STORAGE_BACKEND AND NOT TARGET dep_hiredis)
  add_subdirectory(hiredis)
endif()
