#!/bin/sh

set -eux

dir=build
: ${JOBS:=$(($(getconf _NPROCESSORS_ONLN 2>/dev/null || echo 2) * 2))}
cmake -S . -B "${dir}"
DESTDIR=$(pwd)/install cmake --build "${dir}" --parallel "${JOBS}" --target doc/install
